# Custom-Shell
A Unix-like shell built in C showcasing core OS concepts such as process creation, execution, synchronization, IPC, I/O redirection, and multithreading. It supports built-in and external commands, background execution, piping, command history, a terminal editor, X11 GUI integration, and parallel vector operations using POSIX threads.
